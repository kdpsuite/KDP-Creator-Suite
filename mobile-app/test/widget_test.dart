import 'package:flutter_test/flutter_test.dart';

void main() {
  test('A simple test to ensure the test directory is recognized', () {
    // The expect function checks if the first value equals the second value.
    expect(1 + 1, 2);
  });
}
