{
  "env": {
    "VITE_API_URL": "@api_url"
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://dashboard-backend-hazel.vercel.app/api/:path*"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "env_comment": "Note: VITE_API_URL is set via environment variables in Vercel dashboard. The hardcoded rewrite above is a fallback."
}
